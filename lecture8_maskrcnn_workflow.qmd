---
title: "实例分割 Mask R-CNN 完整工作流程"
subtitle: "从图像标注到模型训练与性能评估"
author: "黄俊豪、高春辉"
date: today
format:
  html:
    toc: true
    toc-depth: 3
    code-fold: false
    code-tools: true
    theme: cosmo
    number-sections: true
execute:
  eval: true
  echo: true
  error: true
---

# 实例分割简介

本项目使用实例分割模型对番茄植株照片进行分析。主要分为数据标注、模型训练、模型推理和性能评估 4 个部分。

**语义分割 vs 实例分割**：

- **语义分割**：像素级分类，所有番茄植株标为同一类
- **实例分割**：像素级分类 + 实例区分，番茄 A、B、C 分别标注

**Mask R-CNN** 是实例分割的经典模型，基于 Faster R-CNN 扩展，包含三个分支：

1. **分类分支**：预测物体类别
2. **边框分支**：回归 bbox
3. **Mask 分支**：输出实例分割 mask


# 环境配置

## 环境配置

本项目使用 **uv** 作为 Python 环境管理工具，推荐在 **Positron** IDE 中完成实验。

首先创建虚拟环境并安装依赖：

```bash
# 创建虚拟环境
uv venv --python 3.10

# 激活环境（Mac/Linux）
source .venv/bin/activate

# 安装依赖
uv pip install -r requirements.txt
```

::: {.callout-tip}
## Positron 配置
在 Positron 中按 `Cmd+Shift+P` (Mac) 或 `Ctrl+Shift+P` (Windows)，输入 "Python: Select Interpreter"，选择 `.venv/bin/python`。
:::

验证环境安装：

```{python}
#| label: verify-environment
import torch
import torchvision
from pycocotools import mask as mask_utils

print(f"PyTorch version: {torch.__version__}")
print(f"Torchvision version: {torchvision.__version__}")
print(f"CUDA available: {torch.cuda.is_available()}")
print("Environment setup successful!")
```


---

# 数据准备

## 项目目录结构

查看当前项目结构：

```
lecture8-maskrcnn/
├── data/   # 数据目录，每个子目录均含有两个文件夹，images 保存图片（jpg 文件），annotations 保存标注（json 文件）
│   ├── example/      # 示例用标注图像和标注数据
│   ├── self/         # 需要自己完成标注的图像和标注保存位置
│   └── test/         # 用于测试的图像和标注
├── .gitignore
├── lecture8_maskrcnn_workflow.qmd
├── README.md
└── requirements.txt
```

## 检查数据文件

验证训练图像是否就位：

```{python}
#| label: check-train-images
import os
from pathlib import Path

TRAIN_DIR = Path("data/self/images")

if TRAIN_DIR.exists():
    train_images = [f for f in TRAIN_DIR.iterdir() if f.is_file() and f.suffix.lower() in ['.jpg', '.jpeg', '.png', '.bmp', '.tif', '.tiff']]
    print(f"训练图像数量: {len(train_images)}")
else:
    print("训练图像不存在")

```

如果训练图像不存在，请到 [此处](data/self/README.md) 按照自己的学号下载自己的文件，解压缩后放到 `data/self/images` 文件夹中。


::: {.callout-note}
## 数据要求
- 训练图像：10-30 张番茄植株图像（JPG/PNG 格式）
- 测试图像：公共测试集，所有学生使用相同数据（随本项目提供）
- 图像分辨率：建议 1000×1000 以上
:::

检查测试数据是否存在：

```{python}
#| label: check-test-images
TEST_DIR = Path("data/test/images")

if TEST_DIR.exists():
    test_images = [f for f in TEST_DIR.iterdir() if f.is_file() and f.suffix.lower() in ['.jpg', '.jpeg', '.png', '.bmp', '.tif', '.tiff']]
    print(f"\n测试图像数量: {len(test_images)}")
    print("\n测试图像列表:")
    for img in sorted(test_images):
        print(f"  - {img.name}")
else:
    print("\n测试图像目录不存在")
```

测试图像随本仓库统一提供，如果没有请到 <https://github.com/D2RS-2026spring/lecture8-mask-r-cnn> 克隆仓库到本地继续。

---

# CVAT 图像标注

## 标注流程概述

使用在线版 CVAT ([https://app.cvat.ai](https://app.cvat.ai)) 进行标注：

1. 注册并登录 CVAT
2. 创建项目：`tomato_instance_segmentation`
3. 创建任务：上传 `data/images/` 中的训练图像
4. 添加标签：`plant`
5. 使用 **Polygon 工具**（快捷键 `N`）逐个标注番茄植株实例
6. 导出标注：选择 **Datumaro 1.0** 格式

::: {.callout-important}
## 标注要点
- **每个植株单独标注**：不要将多个植株合并
- **精确勾勒边缘**：贴合植株边缘，避免包含过多背景
- **处理遮挡**：被遮挡部分也要标注可见区域
- **检查遗漏**：完成后检查是否有漏标
:::

## 导出并放置标注文件

从 CVAT 导出后，将下载得到的压缩包解压并移动到 `data/self/annotations` 文件夹中。

验证标注文件：

```{python}
#| label: verify-annotation-file
from pathlib import Path

ANN_DIR = Path("data/self/annotations")
json_files = list(ANN_DIR.glob("*.json"))
ANN_FILE = None

if json_files:
    global ANN_FILE
    ANN_FILE = json_files[0]
    file_size = ANN_FILE.stat().st_size
    print(f"文件: {ANN_FILE}")
    print(f"大小: {file_size:,} bytes ({file_size/1024:.2f} KB)")
else:
    print(f"目录 {ANN_DIR} 中未找到任何 JSON 格式的标注文件")
```

如果没有找到任何 JSON 格式的标注文件，那么请参照上述提示进行图片标注并将标注结果保存到 `data/self/annotations` 文件夹中。

---

# 标注格式转换

## 转换原理

CVAT 导出的 Datumaro 格式需要转换为 COCO 格式。转换工作：

1. 读取 CVAT 的 RLE 格式 mask
2. 计算每个实例的 bbox 和 area
3. 转换为标准 COCO 格式
4. 输出 `annotations.json`


## 运行转换

首先读取 CVAT 导出的标注文件并初始化 COCO 格式结构：

```{python}
#| label: convert-init-coco
import os
import json
import numpy as np
from pycocotools import mask as mask_utils

INPUT_JSON = ANN_FILE
OUTPUT_JSON = "data/self/annotations/annotations.json"

with open(INPUT_JSON, "r", encoding="utf-8") as f:
    data = json.load(f)

coco = {
    "images": [],
    "annotations": [],
    "categories": [{"id": 1, "name": "tomato", "supercategory": "plant"}]
}
```

遍历每张图像，提取标注信息并计算 bbox 和 area：

```{python}
#| label: convert-process-annotations
ann_id = 1
img_id = 1

for item in data["items"]:
    img = item["image"]
    coco["images"].append({
        "id": img_id,
        "file_name": img["path"],
        "width": img["size"][0],
        "height": img["size"][1]
    })
    
    if "annotations" in item:
        for ann in item["annotations"]:
            if ann["type"] != "mask":
                continue
            
            rle = ann["rle"]
            mask = mask_utils.decode(rle)
            ys, xs = np.where(mask == 1)
            
            if len(xs) > 0:
                x_min, x_max = xs.min(), xs.max()
                y_min, y_max = ys.min(), ys.max()
                bbox = [int(x_min), int(y_min), int(x_max - x_min + 1), int(y_max - y_min + 1)]
                area = int(mask.sum())
            else:
                bbox = [0, 0, 0, 0]
                area = 0
            
            coco["annotations"].append({
                "id": ann_id,
                "image_id": img_id,
                "category_id": 1,
                "segmentation": {"counts": rle["counts"], "size": rle["size"]},
                "bbox": bbox,
                "area": area,
                "iscrowd": 0
            })
            ann_id += 1
    
    img_id += 1
```

保存转换后的 COCO 格式标注文件：

```{python}
#| label: convert-save-coco
os.makedirs(os.path.dirname(OUTPUT_JSON), exist_ok=True)
with open(OUTPUT_JSON, "w", encoding="utf-8") as f:
    json.dump(coco, f, indent=2)

print(f"转换完成！共处理 {len(coco['images'])} 张图像，{len(coco['annotations'])} 个标注")
```

## 验证转换结果

检查生成的 COCO 格式标注：

```{python}
#| label: verify-coco-conversion
import json

# 读取转换后的标注文件
with open(OUTPUT_JSON, 'r') as f:
    coco_data = json.load(f)

print(f"图像数量: {len(coco_data['images'])}")
print(f"标注数量: {len(coco_data['annotations'])}")
print(f"类别数量: {len(coco_data['categories'])}")

print("\n类别信息:")
for cat in coco_data['categories']:
    print(f"  - ID: {cat['id']}, Name: {cat['name']}")

print("\n第一张图像信息:")
print(json.dumps(coco_data['images'][0], indent=2))
```

---

# 模型训练

训练脚本的核心功能包括：

- 自动检测设备（CUDA/CPU）
- 加载 COCO 格式数据
- 使用预训练 Mask R-CNN (ResNet50-FPN)
- 记录训练损失（总损失 + 5 个子损失项）
- 每个 epoch 保存模型权重


## 运行训练

首先定义数据集类和模型构建函数：

```{python}
#| label: train-define-dataset-model
import os
import torch
import random
import numpy as np
from PIL import Image
import torchvision
import torchvision.transforms as T
from torchvision.models.detection.faster_rcnn import FastRCNNPredictor
from pycocotools.coco import COCO
from pycocotools import mask as mask_utils

# 固定随机种子
random.seed(42)
np.random.seed(42)
torch.manual_seed(42)

class COCOTomatoDataset(torch.utils.data.Dataset):
    def __init__(self, img_dir, ANN_FILE, transforms=None):
        self.img_dir = img_dir
        self.coco = COCO(ANN_FILE)
        self.transforms = transforms
        self.ids = list(sorted(self.coco.imgs.keys()))

    def __getitem__(self, index):
        img_id = self.ids[index]
        ann_ids = self.coco.getAnnIds(imgIds=img_id)
        anns = self.coco.loadAnns(ann_ids)
        
        img_info = self.coco.loadImgs(img_id)[0]
        img_path = os.path.join(self.img_dir, img_info["file_name"])
        img = Image.open(img_path).convert("RGB")
        
        boxes = []
        masks = []
        labels = []
        
        for ann in anns:
            if "bbox" not in ann:
                continue
            x, y, w, h = ann["bbox"]
            boxes.append([x, y, x + w, y + h])
            masks.append(mask_utils.decode(ann["segmentation"]))
            labels.append(1)
        
        boxes = torch.as_tensor(boxes, dtype=torch.float32)
        masks = torch.as_tensor(np.array(masks), dtype=torch.uint8)
        labels = torch.as_tensor(labels, dtype=torch.int64)
        
        target = {
            "boxes": boxes,
            "labels": labels,
            "masks": masks,
            "image_id": torch.tensor([img_id]),
            "area": torch.as_tensor([ann["area"] for ann in anns], dtype=torch.float32),
            "iscrowd": torch.zeros((len(anns),), dtype=torch.int64)
        }
        
        if self.transforms:
            img = self.transforms(img)
        
        return img, target

    def __len__(self):
        return len(self.ids)

def get_model(num_classes=2):
    model = torchvision.models.detection.maskrcnn_resnet50_fpn(weights="DEFAULT")
    in_features = model.roi_heads.box_predictor.cls_score.in_features
    model.roi_heads.box_predictor = FastRCNNPredictor(in_features, num_classes)
    return model
```

准备训练数据和模型：

```{python}
#| label: train-prepare-data-model
from torch.utils.data import DataLoader

device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
print(f"Using device: {device}")

transforms = T.Compose([T.ToTensor()])
dataset = COCOTomatoDataset(TRAIN_DIR, OUTPUT_JSON, transforms=transforms)
train_loader = DataLoader(dataset, batch_size=1, shuffle=True, collate_fn=lambda x: tuple(zip(*x)))

model = get_model(num_classes=2)
model.to(device)

params = [p for p in model.parameters() if p.requires_grad]
optimizer = torch.optim.SGD(params, lr=0.005, momentum=0.9, weight_decay=0.0005)
```

执行训练循环：

```{python}
#| label: train-loop
from tqdm import tqdm

num_epochs = 5
iter_loss_history = []
loss_classifier_history = []
loss_box_reg_history = []
loss_mask_history = []
loss_objectness_history = []
loss_rpn_box_reg_history = []

os.makedirs("outputs/checkpoints", exist_ok=True)

for epoch in range(num_epochs):
    model.train()
    epoch_losses = []
    
    for images, targets in tqdm(train_loader, desc=f"Epoch {epoch+1}/{num_epochs}"):
        images = [img.to(device) for img in images]
        targets = [{k: v.to(device) for k, v in t.items()} for t in targets]
        
        loss_dict = model(images, targets)
        losses = sum(loss for loss in loss_dict.values())
        
        optimizer.zero_grad()
        losses.backward()
        optimizer.step()
        
        iter_loss_history.append(losses.item())
        loss_classifier_history.append(loss_dict["loss_classifier"].item())
        loss_box_reg_history.append(loss_dict["loss_box_reg"].item())
        loss_mask_history.append(loss_dict["loss_mask"].item())
        loss_objectness_history.append(loss_dict["loss_objectness"].item())
        loss_rpn_box_reg_history.append(loss_dict["loss_rpn_box_reg"].item())
        epoch_losses.append(losses.item())
    
    avg_loss = np.mean(epoch_losses)
    print(f"Epoch {epoch+1} - Average Loss: {avg_loss:.4f}")
    torch.save(model.state_dict(), f"outputs/checkpoints/model_epoch_{epoch+1}.pth")
```

::: {.callout-note}
## 训练时间估计
- **CPU**：约 2-3 分钟/epoch（20 张图像）
- **GPU**：约 30-60 秒/epoch

训练过程中会显示进度条和每个 epoch 的损失值。
:::

## 查看训练损失曲线

绘制总损失曲线：

```{python}
#| label: fig-loss-total
import matplotlib.pyplot as plt

plt.figure(figsize=(10, 6))
plt.plot(iter_loss_history, label="Total Loss")
plt.xlabel("Iteration")
plt.ylabel("Loss")
plt.title("Total Loss per Iteration")
plt.grid(True)
plt.legend()
plt.show()
```

绘制各损失分量曲线：

```{python}
#| label: fig-loss-components
plt.figure(figsize=(12, 7))
plt.plot(loss_classifier_history, label="loss_classifier")
plt.plot(loss_box_reg_history, label="loss_box_reg")
plt.plot(loss_mask_history, label="loss_mask")
plt.plot(loss_objectness_history, label="loss_objectness")
plt.plot(loss_rpn_box_reg_history, label="loss_rpn_box_reg")
plt.xlabel("Iteration")
plt.ylabel("Loss")
plt.title("Loss Components per Iteration")
plt.grid(True)
plt.legend()
plt.show()
```

**五个损失项说明**：

- `loss_classifier`：分类损失
- `loss_box_reg`：边框回归损失
- `loss_mask`：Mask 分割损失
- `loss_objectness`：RPN 物体性得分损失
- `loss_rpn_box_reg`：RPN 边框回归损失

## 检查保存的模型权重

```{python}
#| label: check-model-weights
from pathlib import Path

CHECKPOINT_DIR = Path("outputs/checkpoints")
if CHECKPOINT_DIR.exists():
    checkpoints = sorted(CHECKPOINT_DIR.glob("*.pth"))
    print(f"保存的模型权重 ({len(checkpoints)} 个):\n")
    for ckpt in checkpoints:
        size_mb = ckpt.stat().st_size / (1024 * 1024)
        print(f"  {ckpt.name:30s}  {size_mb:8.2f} MB")
else:
    print("checkpoints 目录不存在")
```

---

# 模型推理

推理时加载训练好的模型，对测试集进行推理，并可视化预测结果（mask + bbox + 置信度 + 面积）。

## 运行推理

加载训练好的模型：

```{python}
#| label: infer-load-model
import os
import torch
import torchvision
from torchvision.transforms import functional as F
from torchvision.models.detection.faster_rcnn import FastRCNNPredictor
from PIL import Image
import numpy as np
import cv2
from tqdm import tqdm
import json
from pycocotools import mask as maskUtils

# 构建 Mask R-CNN（与训练一致）
def get_model(num_classes=2):
    model = torchvision.models.detection.maskrcnn_resnet50_fpn(weights="DEFAULT")
    in_features = model.roi_heads.box_predictor.cls_score.in_features
    model.roi_heads.box_predictor = FastRCNNPredictor(in_features, num_classes)
    return model

# 加载模型（CPU-only）
weight_path = "outputs/checkpoints/model_epoch_5.pth"

device = torch.device("cpu")
print(f"Using device: {device}")

model = get_model(num_classes=2)
model.load_state_dict(torch.load(weight_path, map_location=device))
model.to(device)
model.eval()

```

对测试集进行推理并评估结果：

```{python}
#| label: infer-evaluate
# IoU 计算（BBox）
def compute_bbox_iou(box1, box2):
    x1, y1, w1, h1 = box1
    x2, y2, w2, h2 = box2

    xa = max(x1, x2)
    ya = max(y1, y2)
    xb = min(x1 + w1, x2 + w2)
    yb = min(y1 + h1, y2 + h2)

    inter = max(0, xb - xa) * max(0, yb - ya)
    union = w1*h1 + w2*h2 - inter
    return inter / union if union > 0 else 0

# IoU 计算（Mask）
def compute_mask_iou(mask_pred, mask_gt):
    inter = np.logical_and(mask_pred, mask_gt).sum()
    union = np.logical_or(mask_pred, mask_gt).sum()
    return inter / union if union > 0 else 0

# 推理 + IoU 评估
TEST_DIR = "data/test/test_images"
TEST_ANNOTATION = "data/test/annotations/annotations.json"

# 加载真实标注
with open(TEST_ANNOTATION, "r", encoding="utf-8") as f:
    gt = json.load(f)

# 按 image_id 分组
gt_by_image = {}
for ann in gt["annotations"]:
    img_id = ann["image_id"]
    if img_id not in gt_by_image:
        gt_by_image[img_id] = []
    gt_by_image[img_id].append(ann)

# 图片列表
exts = [".jpg", ".jpeg", ".png", ".bmp", ".tif", ".tiff"]
img_files = [f for f in os.listdir(TEST_DIR) if os.path.splitext(f)[1].lower() in exts]

bbox_iou_list = []
mask_iou_list = []
inference_results = []

for fname in tqdm(img_files, desc="Inferencing"):
    img_path = os.path.join(TEST_DIR, fname)
    image = Image.open(img_path).convert("RGB")
    img_tensor = F.to_tensor(image).to(device)

    # 推理
    with torch.no_grad():
        outputs = model([img_tensor])[0]

    inference_results.append((fname, image, outputs))

    # 找 image_id
    img_id = next((img["id"] for img in gt["images"] if img["file_name"] == fname), None)
    if img_id is None:
        continue

    gt_anns = gt_by_image.get(img_id, [])

    pred_boxes = outputs["boxes"].cpu().numpy()
    pred_scores = outputs["scores"].cpu().numpy()
    pred_masks = outputs["masks"].cpu().numpy()

    pred_keep = pred_scores > 0.5
    pred_boxes = pred_boxes[pred_keep]
    pred_masks = pred_masks[pred_keep]

    # 对每个 GT 计算最佳 IoU
    for ann in gt_anns:
        gt_box = ann["bbox"]
        gt_mask = maskUtils.decode(ann["segmentation"]).astype(bool)

        best_bbox_iou = 0
        best_mask_iou = 0

        for pb, pm in zip(pred_boxes, pred_masks):
            pb_xywh = [pb[0], pb[1], pb[2]-pb[0], pb[3]-pb[1]]
            best_bbox_iou = max(best_bbox_iou, compute_bbox_iou(gt_box, pb_xywh))

            pm_resized = cv2.resize(pm[0], (gt_mask.shape[1], gt_mask.shape[0]))
            pm_bin = pm_resized > 0.5

            best_mask_iou = max(best_mask_iou, compute_mask_iou(pm_bin, gt_mask))

        bbox_iou_list.append(best_bbox_iou)
        mask_iou_list.append(best_mask_iou)

print("\n======================")
print("评估结果：")
print(f"平均 BBOX IoU: {np.mean(bbox_iou_list):.4f}")
print(f"BBOX IoU > 0.5 准确率: {np.mean(np.array(bbox_iou_list) > 0.5):.4f}")
print("----------------------")
print(f"平均 MASK IoU: {np.mean(mask_iou_list):.4f}")
print(f"MASK IoU > 0.5 准确率: {np.mean(np.array(mask_iou_list) > 0.5):.4f}")
print("======================\n")
```

## 可视化推理结果

显示前几张测试图像的原图和预测结果对比：

```{python}
#| label: fig-visualize-results
import matplotlib.pyplot as plt

def visualize_all_masks(original, gt_masks, pred_masks, fname):
    fig, axes = plt.subplots(1, 3, figsize=(20, 6))

    # 原图
    axes[0].imshow(original)
    axes[0].set_title(f"Original: {fname}", fontsize=14)
    axes[0].axis("off")

    # 真实 mask（叠加所有实例）
    axes[1].imshow(original)
    for m in gt_masks:
        axes[1].imshow(m, alpha=0.4, cmap="Reds")
    axes[1].set_title("Ground Truth Masks (All)", fontsize=14)
    axes[1].axis("off")

    # 预测 mask（叠加所有实例）
    axes[2].imshow(original)
    for m in pred_masks:
        axes[2].imshow(m, alpha=0.4, cmap="Blues")
    axes[2].set_title("Predicted Masks (All)", fontsize=14)
    axes[2].axis("off")

    plt.tight_layout()
    plt.show()
num_display = min(3, len(inference_results))

for idx in range(num_display):
    fname, original, outputs = inference_results[idx]

    # 找 image_id
    img_id = next((img["id"] for img in gt["images"] if img["file_name"] == fname), None)
    if img_id is None:
        continue

    # 所有 GT mask
    gt_anns = gt_by_image[img_id]
    gt_masks = [maskUtils.decode(ann["segmentation"]).astype(bool) for ann in gt_anns]

    # 所有 Pred mask（过滤低分）
    pred_masks_raw = outputs["masks"].cpu().numpy()
    pred_scores = outputs["scores"].cpu().numpy()

    pred_masks = []
    for pm, score in zip(pred_masks_raw, pred_scores):
        if score < 0.5:
            continue
        pm_resized = cv2.resize(pm[0], (gt_masks[0].shape[1], gt_masks[0].shape[0]))
        pred_masks.append(pm_resized > 0.5)

    # 可视化所有 mask
    visualize_all_masks(original, gt_masks, pred_masks, fname)

```

::: {.callout-tip}
## 可视化说明
- **绿色半透明区域**：预测的 mask
- **红色边框**：预测的 bbox
- **文本标注**：置信度分数 + 实例面积（像素数）
:::

---

## 对比不同 epoch 的模型

::: {.callout-note}
如需对比不同 epoch 的模型性能，可以修改上面评估代码块中的模型路径，分别运行评估并保存结果，然后使用下面的代码进行对比。
:::

---

# 可重复性总结

## 固定随机种子

在训练代码中，我们使用了以下方式固定随机种子：

```python
random.seed(42)
np.random.seed(42)
torch.manual_seed(42)
```

**作用**：

- 确保数据加载顺序一致
- 确保模型初始化权重一致
- 确保训练过程可重复

::: {.callout-note}
即使固定种子，不同硬件（CPU/GPU）或 PyTorch 版本可能仍有微小差异。
:::

## 环境版本锁定

查看当前环境的依赖版本：

```{bash}
uv pip freeze
```

锁定精确版本：

```bash
uv pip freeze > requirements.txt
```


## 完整工作流程回顾

```{mermaid}
graph TD
    A[获取未标注图像] --> B[CVAT 标注]
    B --> C[导出 Datumaro 格式]
    C --> D[转换为 COCO 格式]
    D --> E[训练 Mask R-CNN]
    E --> F[模型推理]
    F --> G[性能评估]
    G --> H[结果分析与报告]
```

---

# 总结

恭喜完成本讲实验！你已经掌握了：

✅ 实例分割的基本原理  
✅ CVAT 图像标注工具的使用  
✅ Mask R-CNN 模型的训练与推理  
✅ COCO 格式数据的处理  
✅ 模型性能评估方法  
✅ 可重复性研究的最佳实践  

**下一步探索**：

- 尝试在自己的数据集上应用实例分割
- 探索其他实例分割模型（YOLACT、SOLOv2、Mask2Former）
- 学习模型部署与优化（ONNX、TensorRT）
- 研究数据增强技术提升模型泛化能力
